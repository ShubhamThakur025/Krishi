import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: '#006769',
        },
        secondary: {
            main: '#40A578'
        },
        tertiary : {
            main: "#9DDE8B"
        }
    }
})

export default theme